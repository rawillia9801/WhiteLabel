const EMBED_SCRIPT = "(function () {\n  \"use strict\";\n\n  var script = document.currentScript;\n  if (!script) return;\n\n  var targetId = script.getAttribute(\"data-target\") || \"mydogportal-application\";\n  var root = document.getElementById(targetId);\n  if (!root) {\n    root = document.createElement(\"div\");\n    root.id = targetId;\n    if (script.parentNode) script.parentNode.insertBefore(root, script);\n  }\n\n  root.classList.add(\"mdp-application-root\");\n  var apiOrigin = new URL(script.src, window.location.href).origin;\n  var useDefaultStyles = script.getAttribute(\"data-styles\") !== \"false\";\n\n  function setTheme(name, attribute) {\n    var value = script.getAttribute(attribute);\n    if (value) root.style.setProperty(name, value);\n  }\n\n  setTheme(\"--mdp-primary\", \"data-primary\");\n  setTheme(\"--mdp-accent\", \"data-accent\");\n  setTheme(\"--mdp-radius\", \"data-radius\");\n  setTheme(\"--mdp-font\", \"data-font\");\n  setTheme(\"--mdp-background\", \"data-background\");\n  setTheme(\"--mdp-surface\", \"data-surface\");\n  setTheme(\"--mdp-text\", \"data-text\");\n\n  if (useDefaultStyles && !document.getElementById(\"mydogportal-application-styles\")) {\n    var style = document.createElement(\"style\");\n    style.id = \"mydogportal-application-styles\";\n    style.textContent = [\n      \".mdp-application-root{--mdp-primary:#087f8c;--mdp-accent:#c68b24;--mdp-radius:14px;--mdp-background:#f6f7f3;--mdp-surface:#fff;--mdp-text:#173b38;--mdp-muted:#657976;--mdp-border:#cedbd6;--mdp-font:inherit;color:var(--mdp-text);font-family:var(--mdp-font)}\",\n      \".mdp-application-root *{box-sizing:border-box}\",\n      \".mdp-app{width:100%;display:grid;gap:18px;padding:clamp(18px,3vw,34px);border-radius:var(--mdp-radius);background:var(--mdp-background)}\",\n      \".mdp-header{display:grid;gap:8px}\",\n      \".mdp-eyebrow{color:var(--mdp-accent);font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}\",\n      \".mdp-title{margin:0;color:var(--mdp-text);font-size:clamp(28px,5vw,46px);line-height:1.05}\",\n      \".mdp-intro{max-width:760px;margin:0;color:var(--mdp-muted);font-size:14px;line-height:1.65}\",\n      \".mdp-form{display:grid;gap:16px}\",\n      \".mdp-section{display:grid;gap:15px;padding:clamp(18px,3vw,28px);border:1px solid var(--mdp-border);border-radius:var(--mdp-radius);background:var(--mdp-surface)}\",\n      \".mdp-section-title{margin:0;color:var(--mdp-text);font-size:18px}\",\n      \".mdp-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}\",\n      \".mdp-field{display:grid;gap:6px;color:var(--mdp-text);font-size:13px;font-weight:700}\",\n      \".mdp-wide{grid-column:1/-1}\",\n      \".mdp-input,.mdp-select,.mdp-textarea{width:100%;min-height:46px;padding:10px 12px;border:1px solid var(--mdp-border);border-radius:calc(var(--mdp-radius) * .65);outline:0;background:#fff;color:var(--mdp-text);font:inherit;font-weight:500}\",\n      \".mdp-textarea{min-height:112px;resize:vertical}\",\n      \".mdp-input:focus,.mdp-select:focus,.mdp-textarea:focus{border-color:var(--mdp-primary);box-shadow:0 0 0 3px color-mix(in srgb,var(--mdp-primary) 12%,transparent)}\",\n      \".mdp-help{color:var(--mdp-muted);font-size:11px;font-weight:500;line-height:1.45}\",\n      \".mdp-required{color:#a2473d}\",\n      \".mdp-choices{display:grid;gap:7px}\",\n      \".mdp-choice,.mdp-check{display:flex;align-items:flex-start;gap:9px;padding:11px 12px;border:1px solid var(--mdp-border);border-radius:calc(var(--mdp-radius) * .65);background:#fff;color:var(--mdp-text);font-size:13px;font-weight:600}\",\n      \".mdp-choice input,.mdp-check input{width:17px;height:17px;margin:1px 0 0;accent-color:var(--mdp-primary)}\",\n      \".mdp-actions{display:flex;align-items:center;justify-content:space-between;gap:16px}\",\n      \".mdp-submit{min-height:48px;padding:0 20px;border:0;border-radius:calc(var(--mdp-radius) * .7);background:var(--mdp-primary);color:#fff;font:inherit;font-weight:800;cursor:pointer}\",\n      \".mdp-submit:disabled{opacity:.6;cursor:wait}\",\n      \".mdp-status{display:none;padding:13px 15px;border-radius:calc(var(--mdp-radius) * .7);font-size:13px;line-height:1.5}\",\n      \".mdp-status[data-state=error]{display:block;background:#fff0ee;color:#8d332c}\",\n      \".mdp-status[data-state=success]{display:block;background:#edf8f3;color:#24624e}\",\n      \".mdp-privacy{margin:0;color:var(--mdp-muted);font-size:11px;line-height:1.5}\",\n      \".mdp-honeypot{position:absolute!important;width:1px!important;height:1px!important;overflow:hidden!important;clip:rect(0 0 0 0)!important}\",\n      \"@media(max-width:680px){.mdp-grid{grid-template-columns:1fr}.mdp-wide{grid-column:auto}.mdp-actions{align-items:stretch;flex-direction:column}.mdp-submit{width:100%}}\"\n    ].join(\"\");\n    document.head.appendChild(style);\n  }\n\n  function element(tag, className, text) {\n    var node = document.createElement(tag);\n    if (className) node.className = className;\n    if (text !== undefined && text !== null) node.textContent = String(text);\n    return node;\n  }\n\n  function labelText(field) {\n    var span = element(\"span\", \"mdp-label\", field.label);\n    if (field.required) span.appendChild(element(\"span\", \"mdp-required\", \" *\"));\n    return span;\n  }\n\n  function helpText(field) {\n    return field.help ? element(\"small\", \"mdp-help\", field.help) : null;\n  }\n\n  function renderField(field) {\n    if (field.type === \"checkbox\") {\n      var check = element(\"label\", \"mdp-check mdp-wide\");\n      var checkbox = document.createElement(\"input\");\n      checkbox.type = \"checkbox\";\n      checkbox.name = field.key;\n      checkbox.required = Boolean(field.required);\n      check.appendChild(checkbox);\n      var checkCopy = element(\"span\", \"\", field.label);\n      if (field.required) checkCopy.appendChild(element(\"span\", \"mdp-required\", \" *\"));\n      if (field.help) checkCopy.appendChild(element(\"small\", \"mdp-help\", \" \" + field.help));\n      check.appendChild(checkCopy);\n      return check;\n    }\n\n    if (field.type === \"radio\") {\n      var radioWrap = element(\"div\", \"mdp-field mdp-wide\");\n      radioWrap.appendChild(labelText(field));\n      var choices = element(\"div\", \"mdp-choices\");\n      (field.options || []).forEach(function (option) {\n        var choice = element(\"label\", \"mdp-choice\");\n        var input = document.createElement(\"input\");\n        input.type = \"radio\";\n        input.name = field.key;\n        input.value = option;\n        input.required = Boolean(field.required);\n        choice.appendChild(input);\n        choice.appendChild(document.createTextNode(option));\n        choices.appendChild(choice);\n      });\n      radioWrap.appendChild(choices);\n      var radioHelp = helpText(field);\n      if (radioHelp) radioWrap.appendChild(radioHelp);\n      return radioWrap;\n    }\n\n    var label = element(\"label\", field.type === \"textarea\" ? \"mdp-field mdp-wide\" : \"mdp-field\");\n    label.appendChild(labelText(field));\n    var control;\n\n    if (field.type === \"select\") {\n      control = element(\"select\", \"mdp-select\");\n      control.name = field.key;\n      control.required = Boolean(field.required);\n      var placeholder = element(\"option\", \"\", \"Select one\");\n      placeholder.value = \"\";\n      placeholder.disabled = true;\n      placeholder.selected = true;\n      control.appendChild(placeholder);\n      (field.options || []).forEach(function (option) {\n        var optionNode = element(\"option\", \"\", option);\n        optionNode.value = option;\n        control.appendChild(optionNode);\n      });\n    } else if (field.type === \"textarea\") {\n      control = element(\"textarea\", \"mdp-textarea\");\n      control.name = field.key;\n      control.required = Boolean(field.required);\n    } else {\n      control = element(\"input\", \"mdp-input\");\n      control.type = field.type || \"text\";\n      control.name = field.key;\n      control.required = Boolean(field.required);\n    }\n\n    label.appendChild(control);\n    var help = helpText(field);\n    if (help) label.appendChild(help);\n    return label;\n  }\n\n  function showFailure(message) {\n    root.innerHTML = \"\";\n    var box = element(\"div\", \"mdp-app\");\n    var status = element(\"div\", \"mdp-status\", message || \"Unable to load this application.\");\n    status.setAttribute(\"data-state\", \"error\");\n    box.appendChild(status);\n    root.appendChild(box);\n  }\n\n  async function start() {\n    root.setAttribute(\"aria-busy\", \"true\");\n    var configResponse = await fetch(apiOrigin + \"/api/website/application-config\", {\n      method: \"GET\",\n      headers: { \"accept\": \"application/json\" },\n      credentials: \"omit\"\n    });\n    var payload = await configResponse.json();\n    if (!configResponse.ok || !payload.form) throw new Error(payload.error || \"Unable to load this application.\");\n\n    var formConfig = payload.form;\n    var app = element(\"div\", \"mdp-app\");\n    var header = element(\"header\", \"mdp-header\");\n    header.appendChild(element(\"span\", \"mdp-eyebrow\", payload.kennel && payload.kennel.name ? payload.kennel.name : \"Puppy Application\"));\n    header.appendChild(element(\"h2\", \"mdp-title\", formConfig.title));\n    header.appendChild(element(\"p\", \"mdp-intro\", formConfig.introduction));\n    app.appendChild(header);\n\n    var status = element(\"div\", \"mdp-status\");\n    status.setAttribute(\"role\", \"status\");\n    app.appendChild(status);\n\n    var form = element(\"form\", \"mdp-form\");\n    var honeypot = document.createElement(\"input\");\n    honeypot.type = \"text\";\n    honeypot.name = \"company\";\n    honeypot.tabIndex = -1;\n    honeypot.autocomplete = \"off\";\n    honeypot.className = \"mdp-honeypot\";\n    honeypot.setAttribute(\"aria-hidden\", \"true\");\n    form.appendChild(honeypot);\n\n    var sections = [];\n    (formConfig.fields || []).forEach(function (field) {\n      if (sections.indexOf(field.section) === -1) sections.push(field.section);\n    });\n\n    sections.forEach(function (sectionName) {\n      var section = element(\"section\", \"mdp-section\");\n      section.appendChild(element(\"h3\", \"mdp-section-title\", sectionName));\n      var grid = element(\"div\", \"mdp-grid\");\n      formConfig.fields.filter(function (field) { return field.section === sectionName; }).forEach(function (field) {\n        grid.appendChild(renderField(field));\n      });\n      section.appendChild(grid);\n      form.appendChild(section);\n    });\n\n    var actions = element(\"footer\", \"mdp-actions\");\n    actions.appendChild(element(\"p\", \"mdp-privacy\", \"Your application is sent securely to \" + (payload.kennel && payload.kennel.name ? payload.kennel.name : \"the breeder\") + \" through MyDogPortal for review, workflow automation, and document preparation.\"));\n    var submit = element(\"button\", \"mdp-submit\", formConfig.submitLabel || \"Submit application\");\n    submit.type = \"submit\";\n    actions.appendChild(submit);\n    form.appendChild(actions);\n\n    form.addEventListener(\"submit\", async function (event) {\n      event.preventDefault();\n      submit.disabled = true;\n      status.removeAttribute(\"data-state\");\n      status.textContent = \"\";\n\n      var data = new FormData(form);\n      var application = {};\n      (formConfig.fields || []).forEach(function (field) {\n        application[field.key] = field.type === \"checkbox\" ? data.get(field.key) === \"on\" : String(data.get(field.key) || \"\");\n      });\n      application.company = String(data.get(\"company\") || \"\");\n\n      try {\n        var submissionResponse = await fetch(apiOrigin + \"/api/website/applications\", {\n          method: \"POST\",\n          headers: { \"content-type\": \"application/json\", \"accept\": \"application/json\" },\n          credentials: \"omit\",\n          body: JSON.stringify({ application: application })\n        });\n        var result = await submissionResponse.json();\n        if (!submissionResponse.ok) throw new Error(result.error || \"Unable to submit your application.\");\n        form.reset();\n        status.textContent = formConfig.successMessage || \"Thank you. Your application has been received.\";\n        status.setAttribute(\"data-state\", \"success\");\n        root.dispatchEvent(new CustomEvent(\"mydogportal:application-submitted\", { detail: result, bubbles: true }));\n      } catch (error) {\n        status.textContent = error && error.message ? error.message : \"Unable to submit your application.\";\n        status.setAttribute(\"data-state\", \"error\");\n      } finally {\n        submit.disabled = false;\n      }\n    });\n\n    app.appendChild(form);\n    root.innerHTML = \"\";\n    root.appendChild(app);\n    root.removeAttribute(\"aria-busy\");\n    root.dispatchEvent(new CustomEvent(\"mydogportal:application-ready\", { detail: { form: formConfig }, bubbles: true }));\n  }\n\n  start().catch(function (error) {\n    root.removeAttribute(\"aria-busy\");\n    showFailure(error && error.message ? error.message : \"Unable to load this application.\");\n  });\n})();";

export function GET() {
  return new Response(EMBED_SCRIPT, {
    headers: {
      "content-type": "application/javascript; charset=utf-8",
      "cache-control": "public, max-age=300, s-maxage=300",
      "access-control-allow-origin": "*",
      "x-content-type-options": "nosniff",
    },
  });
}
